      columns = row.split(',')
            # print(columns[0])
            # print(columns[12])
            print(len(columns))
            for k in columns:
                print(k)
            exit(0)